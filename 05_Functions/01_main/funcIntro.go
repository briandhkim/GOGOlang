package main

import "fmt"

/*
can only have one `func main` in the entire program
it needs to be inside `package main`

functions:
start with `func`
receiver if there is one
then function name
then parenthesis

	func sampleFunction() int{}
	`int` is the return type
*/

func main() {
	fmt.Println("hello world")
}
